diff --git a/Cargo.toml b/Cargo.toml
index e40afd7..421a0d0 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -146,9 +146,9 @@ matrix-sdk = { version = "0.17", optional = true, default-features = false }
 tokio-test = "0.4"
 colored = "2"
 
-[[example]]
-name = "demo"
-path = "examples/demo.rs"
+# [[example]]
+# name = "demo"
+# path = "examples/demo.rs"
 
 [[bin]]
 name = "souveraine"
diff --git a/src/bridge/bifrost.rs b/src/bridge/bifrost.rs
index c76ca21..e4e2a8b 100644
--- a/src/bridge/bifrost.rs
+++ b/src/bridge/bifrost.rs
@@ -513,8 +513,9 @@ impl BifrostClient {
                     return Err(primary_err);
                 }
                 warn!(
-                    "Primary model {} exhausted, trying {} fallback(s)",
+                    "Primary model {} exhausted (error: {}), trying {} fallback(s)",
                     request.model,
+                    primary_err,
                     fallbacks.len()
                 );
             }
diff --git a/src/main.rs b/src/main.rs
index 9f5a12a..232c199 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -1080,6 +1080,7 @@ async fn run_reflect(
         &dirs::home_dir()
             .unwrap_or_default()
             .join(".souveraine")
+            .join("server")
             .join("agents")
             .join(&agent.id),
     );
diff --git a/src/ui/chat/mod.rs b/src/ui/chat/mod.rs
index 6f71f68..febfd18 100644
--- a/src/ui/chat/mod.rs
+++ b/src/ui/chat/mod.rs
@@ -350,6 +350,7 @@ pub struct ChatState {
     pub render_mode: ChatMode,
     pub palette: ChatPalette,
     pub stream_buffer: String,
+    pub subconscious_stream: Vec<String>,
 
     /// Current itinerary route-line for the header strip.
     /// Empty string means no active itinerary.
@@ -503,6 +504,7 @@ impl ChatState {
             render_mode: ChatMode::Conversation,
             palette: ChatPalette::default(),
             stream_buffer: String::new(),
+            subconscious_stream: Vec::new(),
             itinerary_line: String::new(),
         })
     }
